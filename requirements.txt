# ================================
# Agentic Threat Analyst — Okta Logs
# Requirements (LangChain v0.3+ compatible)
# Switched to OpenAI-compatible LM Studio endpoint.
# ================================

langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

pyyaml>=6.0.1
tabulate>=0.9.0

# Optional: conceptual autogen demo (no API calls)
autogen>=0.3.0
